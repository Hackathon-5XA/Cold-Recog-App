import { AppRegistry } from 'react-native';
import App from './App';

// Register the application component directly with a hardcoded name
AppRegistry.registerComponent('my-react-native-app', () => App);
